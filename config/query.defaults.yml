server:
  http_port: 8083
  read_timeout: 30s
  write_timeout: 30s
  idle_timeout: 120s

database:
  dsn: "postgres://testuser:secret@postgres:5432/testdb?sslmode=disable"
  max_connections: 30
  min_connections: 5
  max_idle_time: 30m
  max_lifetime: 1h

blockchain:
  enabled: true
  chainmaker_config: /app/config/blockchain.defaults.yml

logging:
  level: info
  format: json
  audit_enabled: true
  audit_file: /var/log/query/audit.log
